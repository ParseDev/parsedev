<!DOCTYPE html>
<html class="h-full bg-white">
  <head>
    <title>Parse.dev</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <% # Add a favicon %>
    <link rel="icon" type="image/png" href="" />
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag asset_path("logo.png") %>
  </head>

  <body class="h-full">
    <% flash.each do |type, message| %>
        <div class="<%= flash_class(type) %>">
          <%= message %>
        </div>
  <% end %>

  <div class="min-h-full">
  <nav class="border-b border-gray-200 bg-white">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
      <div class="flex h-16 justify-between">
        <div class="flex">
          <div class="flex flex-shrink-0 items-center">
          <%= link_to root_path do %>
              <%= image_tag "logo.png", class: "block h-8 w-auto lg:hidden", alt: "ChartGPT" %>
              <%= image_tag "logo.png", class: "hidden h-8 w-auto lg:block", alt: "ChartGPT" %>
            <% end %>
          </div>
          <div class="hidden sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
            <!-- app/views/your_view.html.erb -->
            <%= link_to "Dashboard", root_path, class: "#{active_menu_class(root_path)} inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium", aria: { current: current_page?(root_path) ? "page" : nil } %>
            <%= link_to "Datasources", datasources_path, class: "#{active_menu_class(datasources_path)} inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium", aria: { current: current_page?(datasources_path) ? "page" : nil } %>
            <%= link_to "Saved Queries", dataqueries_path, class: "#{active_menu_class(dataqueries_path)} inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium", aria: { current: current_page?(dataqueries_path) ? "page" : nil } %>
          </div>
        </div>
        <div class="hidden sm:ml-6 sm:flex sm:items-center">

          <!-- Profile dropdown -->
          <div class="relative ml-3">
            <div>
              <button type="button" data-controller="menu" data-action="click->menu#handleClick" class="flex max-w-xs items-center rounded-full bg-white text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" id="user-menu-button" aria-expanded="false" aria-haspopup="true">
                <span class="sr-only">Open user menu</span>
                <%= current_user.email %>
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
                </svg>
              </button>
            </div>

            <!--
              Dropdown menu, show/hide based on menu state.

              Entering: "transition ease-out duration-200"
                From: "transform opacity-0 scale-95"
                To: "transform opacity-100 scale-100"
              Leaving: "transition ease-in duration-75"
                From: "transform opacity-100 scale-100"
                To: "transform opacity-0 scale-95"
            -->
            <div class="absolute right-0 z-10 mt-2 w-48 origin-top-right rounded-md bg-white py-1 shadow-lg ring-1 ring-black ring-opacity-5 focus:outline-none hidden" role="menu" aria-orientation="vertical" aria-labelledby="user-menu-button" tabindex="-1">
              <!-- Active: "bg-gray-100", Not Active: "" -->
              <%= link_to "Your Team", company_users_path(current_user.company), class: "block px-4 py-2 text-sm text-gray-700", role: "menuitem", tabindex: "-1", id: "user-menu-item-0" %>
              <a href="#" class="block px-4 py-2 text-sm text-gray-700" role="menuitem" tabindex="-1" id="user-menu-item-1">Settings</a>
              <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "block px-4 py-2 text-sm text-gray-700", role: "menuitem", tabindex: "-1", id: "user-menu-item-2" %>

            </div>
          </div>
        </div>
        <div class="-mr-2 flex items-center sm:hidden">
          <!-- Mobile menu button -->
          <button type="button" data-controller="mobilemenu" data-action="click->mobilemenu#handleClick" class="inline-flex items-center justify-center rounded-md bg-white p-2 text-gray-400 hover:bg-gray-100 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2" aria-controls="mobile-menu" aria-expanded="false">
            <span class="sr-only">Open main menu</span>
            <!-- Menu open: "hidden", Menu closed: "block" -->
            <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
            </svg>
            <!-- Menu open: "block", Menu closed: "hidden" -->
            <svg class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>
    </div>

    <!-- Mobile menu, show/hide based on menu state. -->
    <div class="hidden" id="mobile-menu">
      <div class="space-y-1 pb-3 pt-2">
        <!-- Current: "border-indigo-500 bg-indigo-50 text-indigo-700", Default: "border-transparent text-gray-600 hover:border-gray-300 hover:bg-gray-50 hover:text-gray-800" -->
        <!-- app/views/your_view.html.erb -->
        <%= link_to "Dashboard", root_path, class: "#{active_mobile_menu_class(root_path)} block border-l-4 py-2 pl-3 pr-4 text-base font-medium", aria: { current: current_page?(root_path) ? "page" : nil } %>
        <%= link_to "Datasources", datasources_path, class: "#{active_mobile_menu_class(datasources_path)} block border-l-4 py-2 pl-3 pr-4 text-base font-medium", aria: { current: current_page?(datasources_path) ? "page" : nil } %>
        <%= link_to "Saved Queries", dataqueries_path, class: "#{active_mobile_menu_class(dataqueries_path)} block border-l-4 py-2 pl-3 pr-4 text-base font-medium", aria: { current: current_page?(dataqueries_path) ? "page" : nil } %>

      </div>
      <div class="border-t border-gray-200 pb-3 pt-4">
        <div class="flex items-center px-4">
          <div class="flex-shrink-0">
            <%= current_user.email %>
          </div>
        </div>
        <div class="mt-3 space-y-1">
          <%= link_to "Your Team", company_users_path(current_user.company), class: "block px-4 py-2 text-base font-medium text-gray-500 hover:bg-gray-100 hover:text-gray-800" %>
          <a href="#" class="block px-4 py-2 text-base font-medium text-gray-500 hover:bg-gray-100 hover:text-gray-800">Settings</a>
          <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "block px-4 py-2 text-base font-medium text-gray-500 hover:bg-gray-100 hover:text-gray-800" %>

        </div>
      </div>
    </div>
  </nav>

  <%- unless @display_chat %>
    <div class="py-10">
      <main>
        <div class="mx-auto max-w-7xl sm:px-6 lg:px-8">
          <%= yield %>
        </div>
      </main>
    </div>
  <% else %>
    <main>
      <%= yield %>  
      </main>
  <% end %>
</div>
  

  </body>
</html>
